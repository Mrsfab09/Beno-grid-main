import "./globals.css";

export const metadata = {
  title: "Frontend Mentor | Beno grid main",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
